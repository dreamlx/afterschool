<h3>新建班级，导入学生</h3>
	<br/>
	<br/>

<% if flash[:error] %>
  <div class="warning"><%= flash[:error] %></div>
<% else %>

	<h4><%= @sc %><br/></h4>
	<% if !@is_old_class %>
		导入完成！
		<br/>
		<br/>
		<% @students.each_line do |l| %>
			<%= l %> <br/>
		<% end %>
	<% end %>

<% end %>
<br/>
<br/>
<%= link_to '回退', tools_import_students_path %>