require 'test_helper'

class ArticleTest < ActiveSupport::TestCase
  test "the truth" do
    # c = Article.count
    # assert_equal(2, c, 'err count')
    # r = WorkReview.find 1
    # assert !r.blank?
  end
end
